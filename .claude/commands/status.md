---
description: Where the SCA project stands right now
---

Report, in under 10 lines, following `.claude/rules/output.md`:

- **Current task** — the ID and title from `NEXT_TASK.md`, and its `STATUS`.
- **Branch** — the current branch, and whether it is behind `main`.
- **Open pull requests** — number, title, and what each is waiting on.
- **Blocked on** — the one thing stopping progress right now.
- **Waiting on a person** — anything needing a yes from Francis or Jeremy.

Use `git status`, `git branch`, and `gh pr list`. Do not guess any of it.
