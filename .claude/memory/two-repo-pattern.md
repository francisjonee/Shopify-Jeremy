# Francis splits every project into an architecture repo and a codebase repo

Seen across the whole GitHub account: `deskline-architecture` / `deskline-codebase`,
`prepemail-architecture` / `prepemail-codebase`, `relaypayroll_Architecture` /
`RelayPayroll-Codebase`, and more.

`Shopify-Jeremy` is the **architecture** half for SCA. The codebase half does not exist
yet.

**Why it matters:** It explains why this repo has no application in it, and it tells you
where implementation work will eventually belong — a separate repo, not this one.

**How to apply:** Never commit application source here. When a codebase repo is needed,
ask Francis to create it rather than creating it yourself. See
[[no-implementation-yet]].
