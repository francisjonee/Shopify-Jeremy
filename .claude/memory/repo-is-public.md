# `Shopify-Jeremy` is public

This architecture repository is public.

**Why it matters:** Anything committed here is readable by anyone. Tokens, database URLs, customer data, order identifiers, or credentials must never be committed.

**How to apply:** Review every diff before committing. Environment variable names are acceptable; values are not. Repository visibility may be changed only by an authorized technical operator when explicitly approved.
